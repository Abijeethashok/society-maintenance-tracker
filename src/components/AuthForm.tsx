"use client";import {useState}from "react";import {useRouter}from "next/navigation";export default function AuthForm({mode}:{mode:"login"|"register"}){const [err,setErr]=useState("");const r=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const body=Object.fromEntries(new FormData(e.currentTarget));const x=await fetch(`/api/auth/${mode}`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const j=await x.json();if(!x.ok)return setErr(j.error.message);r.push(mode==="login"?(j.data.role==="ADMIN"?"/admin":"/dashboard"):"/login");r.refresh()}return <main className="bg"><div className="shell" style={{maxWidth:460,paddingTop:70}}><div className="card"><h1 style={{fontSize:27,fontWeight:800}}>{mode==="login"?"Welcome back":"Create your account"}</h1><p style={{color:"#687386",margin:"6px 0 20px"}}>Use your society account to continue.</p><form onSubmit={submit} style={{display:"grid",gap:13}}>{mode==="register"&&<input className="input" name="name" placeholder="Full name" required/>}<input className="input" type="email" name="email" placeholder="Email address" required/><input className="input" type="password" name="password" minLength={8} placeholder="Password (at least 8 characters)" required/>{err&&<p style={{color:"#dc2626"}}>{err}</p>}<button className="btn">{mode==="login"?"Sign in":"Register"}</button></form></div></div></main>}
