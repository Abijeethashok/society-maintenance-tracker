import {requireUser}from "@/lib/auth";import{redirect}from"next/navigation";import{prisma}from"@/lib/prisma";import ComplaintTable from "@/components/ComplaintTable";import Link from"next/link";
export default async function Dashboard(){const u=await requireUser();if(u.role==="ADMIN")redirect("/admin");const complaints=await prisma.complaint.findMany({where:{residentId:u.id},orderBy:{createdAt:"desc"}});const notices=await prisma.notice.findMany({where:{important:true},orderBy:{createdAt:"desc"},take:3});const n=(s:string)=>complaints.filter(x=>x.status===s).length;return <main className="bg"><div className="shell"><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",marginBottom:24}}><div><h1 style={{fontSize:30,fontWeight:800}}>Hello, {u.name.split(" ")[0]}</h1><p style={{color:"#687386"}}>Here’s the latest on your maintenance requests.</p></div><Link className="btn" href="/complaints/new">+ Create Complaint</Link></div><div style={{display:"grid",gridTemplateColumns:"repeat(auto-fit,minmax(150px,1fr))",gap:14}}>{[["Total",complaints.length],["Open",n("OPEN")],["In progress",n("IN_PROGRESS")],["Resolved",n("RESOLVED")]].map(([a,b])=><div className="card" key={String(a)}><div style={{color:"#687386"}}>{a}</div><div className="stat">{b}</div></div>)}</div><div style={{display:"grid",gridTemplateColumns:"minmax(0,2fr) minmax(280px,1fr)",gap:20,marginTop:20}}><section className="card"><div style={{display:"flex",justifyContent:"space-between"}}><h2 style={{fontWeight:800,fontSize:18}}>Recent complaints</h2><Link href="/complaints" style={{color:"#2563eb"}}>See all</Link></div><ComplaintTable complaints={complaints.slice(0,6)}/></section><section className="card"><h2 style={{fontWeight:800,fontSize:18,marginBottom:12}}>Important notices</h2>{notices.map(n=><article className="important" style={{padding:"8px 12px",marginBottom:10}} key={n.id}><b>{n.title}</b><p style={{fontSize:14,color:"#687386"}}>{n.content}</p></article>)}{!notices.length&&<p style={{color:"#687386"}}>No important notices right now.</p>}</section></div></div></main>}
