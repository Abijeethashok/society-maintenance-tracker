import {requireUser}from"@/lib/auth";import{redirect}from"next/navigation";import NoticeManager from"@/components/NoticeManager";export default async function AdminNotices(){if((await requireUser()).role!=="ADMIN")redirect("/dashboard");return <main className="bg"><div className="shell" style={{maxWidth:850}}><h1 style={{fontSize:30,fontWeight:850,marginBottom:18}}>Manage notices</h1><NoticeManager/></div></main>}
