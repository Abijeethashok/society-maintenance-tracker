import {requireUser}from"@/lib/auth";import{prisma}from"@/lib/prisma";import ComplaintTable from"@/components/ComplaintTable";import Link from"next/link";export default async function Complaints(){const u=await requireUser();const x=await prisma.complaint.findMany({where:u.role==="ADMIN"?{}:{residentId:u.id},include:{resident:{select:{name:true}}},orderBy:{createdAt:"desc"}});return <main className="bg"><div className="shell"><div style={{display:"flex",justifyContent:"space-between",marginBottom:20}}><div><h1 style={{fontSize:30,fontWeight:800}}>Complaints</h1><p style={{color:"#687386"}}>Track every request and its progress.</p></div>{u.role==="RESIDENT"&&<Link className="btn" href="/complaints/new">+ Create Complaint</Link>}</div><div className="card"><ComplaintTable complaints={x} admin={u.role==="ADMIN"}/></div></div></main>}
