import {requireUser}from"@/lib/auth";import{redirect}from"next/navigation";import ComplaintForm from"@/components/ComplaintForm";export default async function NewComplaint(){if((await requireUser()).role!=="RESIDENT")redirect("/admin");return <main className="bg"><div className="shell" style={{maxWidth:760}}><h1 style={{fontSize:30,fontWeight:800,marginBottom:6}}>Create a complaint</h1><p style={{color:"#687386",marginBottom:20}}>Give us enough detail to get it resolved faster.</p><ComplaintForm/></div></main>}
